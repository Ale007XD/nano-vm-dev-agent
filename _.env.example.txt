# nano-vm-dev-agent environment variables
# Copy this file to .env and fill in your values

# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: paths to local repos (used in runner.py repo_path argument)
NANO_VM_REPO_PATH=/path/to/nano_vm
NANO_VM_MCP_REPO_PATH=/path/to/nano-vm-mcp

# Optional: override default LLM model
# NANO_VM_AGENT_MODEL=claude-sonnet-4-20250514
